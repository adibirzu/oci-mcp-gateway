apiVersion: v1
kind: ServiceAccount
metadata:
  name: oci-mcp-sa
  namespace: oci-mcp
  labels:
    app.kubernetes.io/part-of: oci-mcp-gateway
  annotations:
    # OKE workload identity — maps to OCI dynamic group for resource principal auth
    oci.oraclecloud.com/workload-identity: "true"
