apiVersion: v1
kind: Service
metadata:
  name: oci-logan
  namespace: oci-mcp
  labels:
    app: oci-logan
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: oci-logan
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
