apiVersion: v1
kind: Service
metadata:
  name: db-observatory
  namespace: oci-mcp
  labels:
    app: db-observatory
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: db-observatory
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
