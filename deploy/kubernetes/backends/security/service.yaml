apiVersion: v1
kind: Service
metadata:
  name: oci-mcp-security
  namespace: oci-mcp
  labels:
    app: oci-mcp-security
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: oci-mcp-security
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
