# Template — populate with real values via kubectl create secret, not by editing this file.
# DO NOT commit real values here.
#
# Create with:
#   kubectl create secret generic oci-mcp-gateway-secrets \
#     --from-file=jwt-public-key.pem=/path/to/key.pem \
#     --from-literal=static-token="$(openssl rand -hex 32)" \
#     --from-literal=apm-data-key="YOUR_APM_DATA_KEY" \
#     -n oci-mcp --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: oci-mcp-gateway-secrets
  namespace: oci-mcp
  labels:
    app: oci-mcp-gateway
type: Opaque
data:
  jwt-public-key.pem: ""
  static-token: ""
  apm-data-key: ""
